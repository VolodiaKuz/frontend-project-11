// Update every 30 seconds: /feed?unit=second&interval=30
// Example https://allorigins.hexlet.app/get?disableCache=true&url=https://pt.wikipedia.org/
// Возможно вы не отключили кэш на сервере с прокси.

const updateRss = async () => {
};

export default updateRss;
